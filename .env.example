# Mennekes Amtron RPC Server Configuration
# Copy this file to .env and adjust values as needed

# Modbus RTU Configuration
# For Windows: COM3, COM4, etc.
# For Linux: /dev/ttyUSB0, /dev/ttyUSB1, etc.
MODBUS_PORT=COM3

# Modbus communication parameters
# Default: 57600 (can be 9600, 14400, 19200, 28800, 38400, 56000, 57600)
MODBUS_BAUDRATE=57600

# Data bits (default: 8)
MODBUS_DATABITS=8

# Stop bits (default: 2 for no parity, 1 for even/odd parity)
MODBUS_STOPBITS=2

# Parity: none, even, or odd (default: none)
MODBUS_PARITY=none

# Modbus slave ID (default: 1, satellite mode: 50)
MODBUS_SLAVE_ID=1

# Response timeout in milliseconds (default: 1000)
MODBUS_TIMEOUT=1000

# Reconnection interval in milliseconds (default: 5000)
MODBUS_RECONNECT_INTERVAL=5000

# Maximum number of retries for failed operations (default: 3)
MODBUS_MAX_RETRIES=3

# RPC Server Configuration
# Port for JSON-RPC server (default: 8080)
RPC_PORT=8080

# Heartbeat Configuration
# Enable/disable heartbeat transmission (default: true)
# IMPORTANT: Heartbeat must be enabled for charging to work!
HEARTBEAT_ENABLED=true

# Logging Configuration
# Log level: error, warn, info, debug (default: info)
LOG_LEVEL=info

# Log file path (default: logs/amtron-rpc.log)
LOG_FILE=logs/amtron-rpc.log

# Node Environment
# NODE_ENV=production
